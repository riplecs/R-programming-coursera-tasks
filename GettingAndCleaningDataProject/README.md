To run project:

1. Open run_analysis.R and check if you need to change disk name on lines 5-6 or it is ok for you.
2. if so, run the script by typing sourse('run_analysis.R') and text file with tidy data is appearing in 'UCI HAR Dataset' directory.

Notes:

You need to have a library dplyr installed.
